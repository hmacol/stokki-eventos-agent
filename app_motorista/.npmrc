# Mesmo comportamento do `npm install --legacy-peer-deps` usado localmente:
# a árvore do Expo SDK 57 tem um peer opcional (react-native-worklets via
# @expo/ui) que o npm 11 recusa em modo estrito. Vale pro EAS Build também
# (a fase "Install dependencies" lê este arquivo).
legacy-peer-deps=true